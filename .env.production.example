# Copy to backend/.env.production and fill values for production
PORT=4000
JWT_SECRET=replace_with_a_strong_secret
JWT_EXPIRES_IN=7d
DATABASE_FILE=./data/store.sqlite
CORS_ORIGIN=http://localhost    # change to your frontend domain (e.g. https://example.com)
NODE_ENV=production
